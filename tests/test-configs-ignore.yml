name: Test invalid on-resolve-failure input causes failure
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
      GITHUB_SHA: abc1234567890abcdef1234567890abcdef12345
      GITHUB_REPOSITORY: test-owner/test-repo
      GITHUB_RUN_ID: '12345'
      GITHUB_API_URL: https://api.github.com
      GITHUB_SERVER_URL: https://github.com
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a minimal sbt project structure
        run: |
          mkdir -p /tmp/sbt-configs-test/project
          echo 'sbt.version=1.9.7' > /tmp/sbt-configs-test/project/build.properties
          echo 'ThisBuild / scalaVersion := "2.13.12"' > /tmp/sbt-configs-test/build.sbt

      - id: action
        uses: .
        continue-on-error: true
        with:
          working-directory: /tmp/sbt-configs-test
          on-resolve-failure: invalid-value
          token: fake-token-for-testing

      - name: Assert action failed due to invalid on-resolve-failure value
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "action outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail with invalid on-resolve-failure value, but outcome was '$OUTCOME'"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid on-resolve-failure value"
